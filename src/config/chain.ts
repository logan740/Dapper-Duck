import { abstractTestnet } from "viem/chains";

/**
 * Chain configuration for the Dapper Duck game
 * Currently using Abstract Testnet for both development and production
 * Change to mainnet when ready for production deployment
 */
export const chain = abstractTestnet;
